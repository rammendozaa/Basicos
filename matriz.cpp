#include <iostream>
#include <vector>
using namespace std;
int main()
{
	int n,m;
	cin>>n>>m;
	vector<vector<char> > m1(n,vector<char>(n));
	vector<vector<char> > m2(m,vector<char>(m));
	for
	return 0;
}

